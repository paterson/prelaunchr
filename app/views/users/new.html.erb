<div class="hero">
  <p class="large brandon">Welcome to Hello Sparrow</p>
  <p class="small brandon">Sign up for a free premium subscripton <br> or a chance to win a free flight!</p>
</div>

<div class="form-wrap clearfix <%= "ended" if Rails.application.config.ended %>">
  <% if Rails.application.config.ended %>
    <%= render "users/campaign_ended" %>
  <% else %>
    <%= render "users/campaign_ongoing" %>
  <% end %>
</div>
