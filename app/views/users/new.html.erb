<div class="hero">
  <p class="large brandon">Save hundreds of dollars on your next flight.</p>
  <p class="small brandon">
    Sparrow finds mistake airfares<br>
    and sends them directly to you<br>
    saving you hundreds of dollars<br>
    on flights.
  </p>
  <div class="flight-container">
    <div class="ticket-background"></div>
  </div>
  <div class="logo"></div>
  <span class=" scroll-down visible-xs visible-sm glyphicon glyphicon-chevron-down"></span>
</div>

<div class="form-wrap clearfix <%= "ended" if Rails.application.config.ended %>">
  <% if Rails.application.config.ended %>
    <%= render "users/campaign_ended" %>
  <% else %>
    <%= render "users/campaign_ongoing" %>
  <% end %>
</div>
